type Card = {
  id: number;
  tipText: string;
  tipBackground: string;
  imageSrc: string;
  title: string;
  features: string[],
  priceOnline: number,
  priceOffline: number
};

export type {Card};
